import { Button } from "heroui-native";
import { View } from "react-native";

export default function ExercisesTabs() {
  return (
    <View>
      <Button>
        <Button.Label>Geist</Button.Label>
      </Button>
    </View>
  );
}
